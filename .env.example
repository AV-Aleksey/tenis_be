PORT=3000

# docker compose подхватывает этот файл (env_file); для контейнера app DB_HOST переопределяется на имя сервиса db.
DB_HOST=localhost
DB_PORT=5432
DB_LOGIN=USER
DB_PASSWORD=PASSWORD
DB_NAME=tenis
